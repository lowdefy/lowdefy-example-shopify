id: customers
type: PageSiderMenu
properties:
  title: Customers

requests:
  - _ref: customers/requests/customer_search_results.yaml

blocks:
  Content:
    - id: content_grid
      type: Box
      style:
        maxWidth: 1300px
      blocks:
        - _ref: customers/tiles/search.yaml
        - _ref: customers/tiles/top_customers.yaml
        - id: test
          type: Paragraph
          properties:
            content:
              _stringify:
                - _if:
                    test:
                      _eq:
                        - _state: search_input
                        - null
                    then:
                      $match: {}
                    else:
                      $match:
                        $text:
                          $search:
                            _state: search_input

