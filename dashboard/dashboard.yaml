id: dashboard
type: page_header_menu
properties:
  title: Dashboard
requests:
  - _ref: dashboard/requests/init.yaml
  - _ref: dashboard/requests/totals.yaml
  - _ref: dashboard/requests/day_hour_heatmap.yaml
actions:
  onInit:
    - id: fetch_init
      type: fetch()
      params: init
    - id: set_init
      type: set()
      params:
        start_month:
          _date: '2019-03-01'
        end_month:
          _get:
            key: 0.end_month
            on:
              _request: init
blocks:
  content:
    - _ref: dashboard/totals.yaml
    - _ref: dashboard/filter.yaml
    - _ref: dashboard/day_hour_heatmap.yaml
